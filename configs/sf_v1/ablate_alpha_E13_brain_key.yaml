# E13: brain + keyframe — zero txt, mot channels
model:
  sampler_config:
    params:
      path_b:
        enabled: true
        t_emb_dim: 256
        alpha_zero_channels: ["alpha_txt", "alpha_mot"]
