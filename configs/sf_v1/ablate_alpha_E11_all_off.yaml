# Tier 3 E11: All Channels Off — zero all alphas, pure z_b only
model:
  sampler_config:
    params:
      path_b:
        enabled: true
        t_emb_dim: 256
        alpha_zero_channels: ["alpha_key", "alpha_txt", "alpha_mot", "alpha_brain"]
